# scalatra-playground Repository

## Build & Run

```sh
# Start docker compose
cd docker
docker-compose up -d
docker exec -it scala sh
# Build and run app
sbt "~reStart"
```

## Open App
Go to[http://localhost:8080/](http://localhost:8080/) in your browser.
